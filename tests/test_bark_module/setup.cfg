[metadata]
name = test-bark-core
version = 0.0.1
description = "Test Core Bark rules and commands",

[options]
packages = find:

[options.entry_points]
bark_rules =
	always_fail = test_bark_core.rules:AlwaysFailRule
    always_pass = test_bark_core.rules:AlwaysPassRule